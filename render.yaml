# Render blueprint. In Render: New → Blueprint → point at this repo.
# Using Supabase instead of Render Postgres? Delete the `databases` block below and
# set DATABASE_URL yourself in the dashboard.
services:
  - type: web
    name: gym-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_MODEL
        value: gpt-4o-mini
      - key: OPENAI_VISION_MODEL
        value: gpt-4o-mini
      - key: OPENAI_TRANSCRIBE_MODEL
        value: whisper-1
      - key: TIMEZONE
        value: Europe/Riga
      - key: DIGEST_ENABLED
        value: "true"
      - key: DIGEST_HOUR
        value: "19"
      - key: DIGEST_WEEKLY_DAY
        value: "0"
      - key: SESSION_SECRET
        generateValue: true          # Render generates a strong secret
      - key: DATABASE_URL
        fromDatabase:
          name: gym-db
          property: connectionString
      # --- set these in the dashboard (Environment tab); never commit real values ---
      - key: OPENAI_API_KEY
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_WEBHOOK_SECRET
        sync: false
      - key: ALLOWED_TELEGRAM_USER_ID
        sync: false
      - key: AUTH_USERNAME
        sync: false
      - key: AUTH_PASSWORD_HASH
        sync: false
      # Optional — only needed if you import rides from Strava.
      - key: STRAVA_CLIENT_ID
        sync: false
      - key: STRAVA_CLIENT_SECRET
        sync: false
      - key: STRAVA_REFRESH_TOKEN
        sync: false

databases:
  - name: gym-db
    plan: free
